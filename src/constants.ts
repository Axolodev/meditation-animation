const fps = 60;
const seconds = 16;
const durationInFrames = fps * seconds;
const loops = 10;
const stepDurationSeconds = seconds / 4;

const constants = {
  fps,
  seconds,
  durationInFrames,
  loops,
  stepDurationSeconds,
};

export default constants;
